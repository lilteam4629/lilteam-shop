(()=>{const root=document.querySelector('[data-public-ranger-catalog]');if(!root)return;const grid=root.querySelector('[data-rv-grid]'),input=root.querySelector('input'),count=root.querySelector('[data-rv-count]'),more=root.querySelector('[data-rv-more]');let view='all',page=1,pages=1,timer;const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(reset=false){if(reset){page=1;grid.innerHTML=''}more.disabled=true;const query=new URLSearchParams({view,page:String(page),limit:'60',q:input.value});try{const response=await fetch('/api/rangers-catalog?'+query);if(!response.ok)throw Error('catalog');const data=await response.json();pages=data.pages;grid.insertAdjacentHTML('beforeend',data.items.map(r=>`<article><img src="${esc(r.imageUrl)}" alt="${esc(r.name)}" loading="lazy"><b>${r.grade}★</b><span>${esc(r.name)}</span><small>${esc(r.form)}</small></article>`).join(''));count.textContent=`${data.total.toLocaleString()} ตัวละคร`;more.hidden=page>=pages;more.disabled=false}catch{count.textContent='โหลดคลังไม่สำเร็จ';more.hidden=true}}root.querySelectorAll('[data-rv-view]').forEach(button=>button.addEventListener('click',()=>{view=button.dataset.rvView;root.querySelectorAll('[data-rv-view]').forEach(x=>x.classList.toggle('is-active',x===button));load(true)}));input.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(()=>load(true),250)});more.addEventListener('click',()=>{if(page<pages){page++;load()}});load(true)})();
